<!DOCTYPE html>
<html {{ is_rtl ? 'dir="rtl"' : '' }}>
<head>
    <meta charset="UTF-8"/>
    <title>{{ title }}</title>
</head>
<body style="margin:0">
{% for paper in papers %}

    {{ paper }}

{% endfor %}
</body>
</html>